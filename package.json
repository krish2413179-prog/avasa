{
  "name": "propchain-contracts",
  "version": "1.0.0",
  "description": "PropChain AI Real Estate Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:local": "hardhat run shared/contracts/deploy-properties.js --network localhost",
    "deploy:baseSepolia": "hardhat run shared/contracts/deploy-properties.js --network baseSepolia",
    "test": "hardhat test",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.0",
    "dotenv": "^16.0.0"
  }
}